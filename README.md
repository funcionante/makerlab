# MakerLab's Site

## Instructions

### Set up
* Install [`rvm`](https://rvm.io/)
* `$ source ~/.rvm/scripts/rvm`
* `$ gem install bundle`

### To compile the site
* `$ bundle update`
* `$ jekyll build -d public`
* Visit `public` however you like
